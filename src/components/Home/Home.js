import Hero from '../Hero/Hero';
import Lists from '../Lists/Lists';
import SearchForm from '../SearchForm/SearchForm';

const Home = () => {
  return (
    <div>
      <Hero />
      <SearchForm />
      <Lists />
    </div>
  );
};

export default Home;
